[project]
name = "ai-ux-qa-ai-worker"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
  "openai>=1.101.0",
  "pydantic>=2.11.7"
]

[dependency-groups]
dev = ["pytest>=8.3.0"]

[tool.pytest.ini_options]
pythonpath = ["app"]
testpaths = ["tests"]
